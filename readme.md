# Simple CI Demo

This project shows how Git branches and Jenkins Pipelines work together.

## Prerequisites
- Git installed
- Jenkins server with Git plugin and a job pointing to this repo
- Python 3

## Getting Started
1. **Clone**:
   ```bash
   git clone https://your.git.server/simple-ci-demo.git
   cd simple-ci-demo


- commit git commit -m "Add personalized greeting"
